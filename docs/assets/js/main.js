"use strict";const searchText=document.querySelector(".js_search-input"),searchButton=document.querySelector(".js-search-button"),listSeries=document.querySelector(".js-searchResult"),requestPanel=document.querySelector(".js-form");let series=[],favorites=[];function handleSearch(){apiRequest(searchText.value)}function apiRequest(e){fetch("//api.tvmaze.com/search/shows?q="+e).then(e=>e.json()).then(e=>{const s=e;series=[];for(const e of s)series.push(e.show);paintSeries()})}function paintSeries(){listSeries.innerHTML="";let e="";for(const s of series)e=`<li id=${s.id} class="searchResult_elem js-searchResult_elem">`,null===s.image?e+=`<img class="searchResult_elem-img" \n        src="https://via.placeholder.com/210x295/ffffff/666666/?text=TV" \n        title=${s.name} alt=${s.name}/>`:e+=`<img class="searchResult_elem-img"\n        src=${s.image.medium}\n        title=${s.name} alt=${s.name}/>`,e+=`<h3>${s.name}</h3>`,e+="</li>",listSeries.innerHTML+=e}const seriesCards=document.querySelectorAll(".js-searchResult_elem");for(const e of seriesCards)e.addEventListener("click",showFavorites);function showFavorites(e){e.currentTarget}function handleForm(e){e.preventDefault()}searchButton.addEventListener("click",handleSearch),requestPanel.addEventListener("submit",handleForm);